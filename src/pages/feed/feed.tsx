import { FC } from 'react';

export const Feed: FC = () =>
  // if (!orders.length) {
  //   return <Preloader />;
  // }

  // return <FeedUI orders={orders} handleGetFeeds={handleGetFeeds} />;

  null;
